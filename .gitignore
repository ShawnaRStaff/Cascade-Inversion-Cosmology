.venv/
__pycache__/
*.pyc
.pytest_cache/
.claude/
.DS_Store
._*

# Credentials — NEVER commit
*.pem
*.key
*_accessKeys.csv
*credentials*.csv
*credentials*.json
.aws/
aws_config.local
.env
.env.*

# Ephemeral AWS state (instance IDs, DNS — not secrets, but not for git)
.aws_state
.aws_state.last_*
.aws_fleet_state
.aws_fleet_state.last_*
